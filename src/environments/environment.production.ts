// DO NOT ADD SECRETS HERE
// Add them to the environment variables of the hosting service.
// Then include them in the environment.json file.
export const environment = {
	production: true,
	version: "1.0.2",
	versionName: "Peppy Platypus"
};
